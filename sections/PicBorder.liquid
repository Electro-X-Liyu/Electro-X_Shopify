{% schema %}
{
  "name": "All Product",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "标题",
      "default": "产品展示"
    },
    {
      "type": "range",
      "id": "desktop_height",
      "label": "PC端图片高度(px)",
      "min": 200,
      "max": 800,
      "step": 10,
      "default": 400
    },
    {
      "type": "range",
      "id": "mobile_height",
      "label": "移动端图片高度(px)",
      "min": 150,
      "max": 500,
      "step": 10,
      "default": 300
    }
  ],
  "blocks": [
    {
      "type": "image_block",
      "name": "图片块",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "图片"
        },
        {
          "type": "url",
          "id": "link",
          "label": "链接"
        },
        {
          "type": "text",
          "id": "alt_text",
          "label": "替代文本",
          "info": "图片描述(SEO优化用)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "All Product",
      "category": "产品展示",
      "blocks": [
        {"type": "image_block"},
        {"type": "image_block"},
        {"type": "image_block"},
        {"type": "image_block"},
        {"type": "image_block"}
      ]
    }
  ]
}
{% endschema %}

<div class="all-product-gallery" data-section-id="{{ section.id }}">
  {% if section.settings.title != blank %}
    <h2 class="section-title">{{ section.settings.title }}</h2>
  {% endif %}
  
  <div class="gallery-container">
    <!-- PC端网格布局 -->
    <div class="gallery-grid desktop-gallery">
      {% for block in section.blocks %}
        {% if block.settings.image != blank %}
          {% assign aspect_ratio = block.settings.image.aspect_ratio %}
          <div class="image-card" style="--aspect-ratio: {{ aspect_ratio }}; flex: 0 0 calc({{ aspect_ratio }} * {{ section.settings.desktop_height }}px);" {{ block.shopify_attributes }}>
            <a href="{{ block.settings.link | default: '#' }}" class="image-link">
              <div class="image-wrapper" style="height: {{ section.settings.desktop_height }}px;">
                <img src="{{ block.settings.image | img_url: '1200x' }}"
                     alt="{{ block.settings.alt_text | default: 'Image' }}"
                     class="gallery-image"
                     loading="lazy"
                     width="1200"
                     height="{{ 1200 | divided_by: aspect_ratio | round }}"
                     style="height: 100%; width: auto; max-width: none; object-fit: contain;">
              </div>
            </a>
          </div>
        {% endif %}
      {% endfor %}
    </div>
    
    <!-- 移动端滑动布局 -->
    <div class="mobile-gallery-container">
      <div class="mobile-slider-wrapper">
        <div class="mobile-slider">
          {% for block in section.blocks %}
            {% if block.settings.image != blank %}
              {% assign aspect_ratio = block.settings.image.aspect_ratio %}
              <div class="mobile-slide" style="width: calc({{ aspect_ratio }} * {{ section.settings.mobile_height }}px + 16px); padding: 0 8px;">
                <a href="{{ block.settings.link | default: '#' }}" class="image-link">
                  <div class="image-wrapper" style="height: {{ section.settings.mobile_height }}px;">
                    <img src="{{ block.settings.image | img_url: '800x' }}"
                         alt="{{ block.settings.alt_text | default: 'Image' }}"
                         class="gallery-image"
                         loading="lazy"
                         width="800"
                         height="{{ 800 | divided_by: aspect_ratio | round }}"
                         style="height: 100%; width: auto; max-width: none; object-fit: contain;">
                  </div>
                </a>
              </div>
            {% endif %}
          {% endfor %}
          <!-- 循环滑动需要的副本 -->
          {% for block in section.blocks limit: 2 %}
            {% if block.settings.image != blank %}
              {% assign aspect_ratio = block.settings.image.aspect_ratio %}
              <div class="mobile-slide" style="width: calc({{ aspect_ratio }} * {{ section.settings.mobile_height }}px + 16px); padding: 0 8px;">
                <a href="{{ block.settings.link | default: '#' }}" class="image-link">
                  <div class="image-wrapper" style="height: {{ section.settings.mobile_height }}px;">
                    <img src="{{ block.settings.image | img_url: '800x' }}"
                         alt="{{ block.settings.alt_text | default: 'Image' }}"
                         class="gallery-image"
                         loading="lazy"
                         width="800"
                         height="{{ 800 | divided_by: aspect_ratio | round }}"
                         style="height: 100%; width: auto; max-width: none; object-fit: contain;">
                  </div>
                </a>
              </div>
            {% endif %}
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.all-product-gallery {
  width: 100%;
  margin: 0 auto;
  padding: 0;
}

.section-title {
  text-align: center;
  margin-bottom: 30px;
  font-size: 28px;
  color: #333;
  position: relative;
  padding-bottom: 15px;
}

.section-title:after {
  content: "";
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 80px;
  height: 2px;
  background-color: #333;
}

/* PC端样式 */
.gallery-grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 33px;
  width: 100%;
  max-width: 1440px;
  margin: 0 auto;
  padding: 0 20px;
  box-sizing: border-box;
}

.image-card {
  position: relative;
  transition: transform 0.3s ease;
  margin: 0;
  padding: 0;
}

.image-wrapper {
  position: relative;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  background: transparent;
  width: 100%;
  height: 100%;
}

.gallery-image {
  height: 100%;
  width: auto;
  max-width: none;
  object-fit: contain;
  transition: transform 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  will-change: transform;
}

/* PC端悬停效果 */
.desktop-gallery .image-card:hover .gallery-image {
  transform: scale(1.15);
  z-index: 10;
}

/* 移动端样式 */
.mobile-gallery-container {
  display: none;
  width: 100%;
  position: relative;
  padding: 0;
}

.mobile-slider-wrapper {
  width: 100%;
  overflow: hidden;
  padding: 0;
}

.mobile-slider {
  display: flex;
  transition: transform 0.5s ease;
  padding: 10px 8px;
  will-change: transform;
  margin: 0 -8px;
}

.mobile-slide {
  flex-shrink: 0;
  scroll-snap-align: start;
  box-sizing: border-box;
}

.mobile-slide .image-wrapper {
  overflow: hidden;
  border-radius: 4px;
  height: 100%;
}

/* 响应式切换 */
@media (min-width: 769px) {
  .mobile-gallery-container {
    display: none !important;
  }
}

@media (max-width: 768px) {
  .gallery-grid {
    display: none;
  }
  
  .mobile-gallery-container {
    display: block;
  }
  
  .section-title {
    font-size: 24px;
    margin-bottom: 20px;
    padding: 0 16px;
  }
}

@media (max-width: 480px) {
  .section-title {
    font-size: 20px;
    padding: 0 12px;
  }
  
  .mobile-slider {
    padding: 8px 4px;
    margin: 0 -4px;
  }
  
  .mobile-slide {
    padding: 0 4px !important;
    width: calc({{ aspect_ratio }} * {{ section.settings.mobile_height }}px + 8px) !important;
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const mobileSlider = document.querySelector('.mobile-slider');
  
  if (mobileSlider && mobileSlider.children.length > 1) {
    const slides = mobileSlider.children;
    const slideCount = slides.length - 2;
    const slideWidth = slides[0].offsetWidth;
    
    // 初始化位置
    mobileSlider.style.transform = `translateX(0)`;
    
    let isDragging = false;
    let startPos = 0;
    let currentTranslate = 0;
    let prevTranslate = 0;
    let animationID;
    let currentIndex = 0;
    
    // 触摸事件
    mobileSlider.addEventListener('touchstart', touchStart, { passive: false });
    mobileSlider.addEventListener('touchend', touchEnd, { passive: true });
    mobileSlider.addEventListener('touchmove', touchMove, { passive: false });
    
    // 鼠标事件
    mobileSlider.addEventListener('mousedown', touchStart);
    mobileSlider.addEventListener('mouseup', touchEnd);
    mobileSlider.addEventListener('mouseleave', touchEnd);
    mobileSlider.addEventListener('mousemove', touchMove);
    
    function touchStart(e) {
      isDragging = true;
      startPos = getPositionX(e);
      animationID = requestAnimationFrame(animation);
      mobileSlider.style.cursor = 'grabbing';
      mobileSlider.style.transition = 'none';
      e.preventDefault();
    }
    
    function touchEnd() {
      isDragging = false;
      cancelAnimationFrame(animationID);
      
      const movedBy = currentTranslate - prevTranslate;
      
      if (movedBy < -30) {
        currentIndex += 1;
      } else if (movedBy > 30) {
        currentIndex -= 1;
      }
      
      // 循环逻辑
      if (currentIndex < 0) {
        currentIndex = slideCount - 1;
      } else if (currentIndex >= slideCount) {
        currentIndex = 0;
      }
      
      setSliderPosition();
      mobileSlider.style.cursor = 'grab';
      mobileSlider.style.transition = 'transform 0.5s ease';
    }
    
    function touchMove(e) {
      if (isDragging) {
        const currentPosition = getPositionX(e);
        currentTranslate = prevTranslate + currentPosition - startPos;
        e.preventDefault();
      }
    }
    
    function animation() {
      setSliderPosition();
      if (isDragging) requestAnimationFrame(animation);
    }
    
    function setSliderPosition() {
      const maxTranslate = 0;
      const minTranslate = -(slideWidth * (slideCount + 1));
      currentTranslate = Math.max(minTranslate, Math.min(maxTranslate, currentTranslate));
      mobileSlider.style.transform = `translateX(calc(${currentTranslate}px - 8px))`;
    }
    
    function getPositionX(e) {
      return e.type.includes('mouse') ? e.clientX : e.touches[0].clientX;
    }
  }
  
  // 确保所有图片高度一致
  function enforceEqualHeights() {
    // PC端
    const desktopImages = document.querySelectorAll('.desktop-gallery .image-wrapper');
    if (desktopImages.length > 0) {
      const firstHeight = desktopImages[0].offsetHeight;
      desktopImages.forEach(wrapper => {
        const img = wrapper.querySelector('img');
        if (img) {
          img.style.height = `${firstHeight}px`;
        }
      });
    }
    
    // 移动端
    const mobileImages = document.querySelectorAll('.mobile-slide .image-wrapper');
    if (mobileImages.length > 0) {
      const firstMobileHeight = mobileImages[0].offsetHeight;
      mobileImages.forEach(wrapper => {
        const img = wrapper.querySelector('img');
        if (img) {
          img.style.height = `${firstMobileHeight}px`;
        }
      });
    }
  }
  
  // 初始化执行一次
  enforceEqualHeights();
  
  // 窗口大小改变时重新检查
  window.addEventListener('resize', enforceEqualHeights);
});
</script>