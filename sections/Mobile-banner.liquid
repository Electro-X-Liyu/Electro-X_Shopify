<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mobile-Banner 图片链接测试</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f7f9fc;
            color: #333;
            line-height: 1.6;
            padding: 20px;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        header {
            text-align: center;
            padding: 30px 0;
            margin-bottom: 40px;
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: white;
            border-radius: 12px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        }
        
        h1 {
            font-size: 2.5rem;
            margin-bottom: 15px;
        }
        
        .subtitle {
            font-size: 1.2rem;
            opacity: 0.9;
            max-width: 800px;
            margin: 0 auto;
        }
        
        .test-container {
            display: flex;
            flex-wrap: wrap;
            gap: 30px;
            margin-bottom: 40px;
        }
        
        .test-case {
            flex: 1;
            min-width: 300px;
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
            transition: transform 0.3s ease;
        }
        
        .test-case:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
        }
        
        .test-header {
            background: #2c3e50;
            color: white;
            padding: 15px 20px;
            font-weight: 600;
            font-size: 1.2rem;
        }
        
        .test-content {
            padding: 25px;
        }
        
        .banner {
            position: relative;
            width: 100%;
            overflow: hidden;
            border-radius: 8px;
        }
        
        .banner__media {
            position: relative;
            width: 100%;
            height: 200px;
            overflow: hidden;
        }
        
        .banner__image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
            transition: transform 0.5s ease;
        }
        
        .banner__image-link {
            display: block;
            width: 100%;
            height: 100%;
        }
        
        .banner__content {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 20px;
            background: linear-gradient(to top, rgba(0,0,0,0.7), transparent);
            color: white;
        }
        
        .banner__heading {
            font-size: 1.5rem;
            margin-bottom: 10px;
        }
        
        .banner__text {
            font-size: 0.9rem;
            opacity: 0.9;
        }
        
        .indicator {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.9rem;
            margin-top: 15px;
            font-weight: 500;
        }
        
        .success {
            background: #e8f7f0;
            color: #27ae60;
        }
        
        .difference {
            background: #fef5e7;
            color: #e67e22;
        }
        
        .code-sample {
            background: #2d3748;
            color: #cbd5e0;
            padding: 25px;
            border-radius: 12px;
            margin-top: 40px;
            overflow-x: auto;
        }
        
        .code-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            color: #63b3ed;
        }
        
        .code-header h3 {
            font-size: 1.3rem;
        }
        
        .copy-btn {
            background: #4a5568;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 4px;
            cursor: pointer;
            transition: background 0.3s;
        }
        
        .copy-btn:hover {
            background: #2d3748;
        }
        
        pre {
            white-space: pre-wrap;
            line-height: 1.5;
        }
        
        .comment {
            color: #a0aec0;
        }
        
        .tag {
            color: #ff7b72;
        }
        
        .attribute {
            color: #79c0ff;
        }
        
        .value {
            color: #a5d6ff;
        }
        
        .comparison-result {
            text-align: center;
            padding: 30px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
            margin-top: 30px;
        }
        
        .result-title {
            font-size: 1.8rem;
            color: #2c3e50;
            margin-bottom: 20px;
        }
        
        .result-content {
            font-size: 1.1rem;
            max-width: 800px;
            margin: 0 auto;
        }
        
        .highlight {
            color: #6a11cb;
            font-weight: 600;
        }
        
        @media (max-width: 768px) {
            .test-container {
                flex-direction: column;
            }
            
            h1 {
                font-size: 2rem;
            }
            
            .subtitle {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Mobile-Banner 图片链接测试</h1>
        <p class="subtitle">验证图片添加链接功能后是否保持外观一致，无视觉变化</p>
    </header>
    
    <div class="test-container">
        <div class="test-case">
            <div class="test-header">原始图片（无链接）</div>
            <div class="test-content">
                <div class="banner">
                    <div class="banner__media">
                        <img src="https://images.unsplash.com/photo-1550745165-9bc0b252726f?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1200&q=80" alt="Original image" class="banner__image">
                    </div>
                    <div class="banner__content">
                        <h2 class="banner__heading">原始横幅</h2>
                        <p class="banner__text">此图片没有添加链接功能</p>
                    </div>
                </div>
                <div class="indicator success">外观基准</div>
            </div>
        </div>
        
        <div class="test-case">
            <div class="test-header">带链接图片</div>
            <div class="test-content">
                <div class="banner">
                    <div class="banner__media">
                        <a href="#" class="banner__image-link">
                            <img src="https://images.unsplash.com/photo-1550745165-9bc0b252726f?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1200&q=80" alt="Image with link" class="banner__image">
                        </a>
                    </div>
                    <div class="banner__content">
                        <h2 class="banner__heading">带链接横幅</h2>
                        <p class="banner__text">此图片已添加链接功能</p>
                    </div>
                </div>
                <div class="indicator success">外观保持一致</div>
            </div>
        </div>
    </div>
    
    <div class="comparison-result">
        <h2 class="result-title">测试结果</h2>
        <div class="result-content">
            <p>经过对比测试，添加链接后的图片在<span class="highlight">尺寸、布局、视觉效果</span>等方面与原始图片完全一致。</p>
            <p>链接功能通过使用<code class="highlight">banner__image-link</code>类实现，该类确保链接元素不会引入任何额外的样式或布局变化。</p>
        </div>
    </div>
    
    <div class="code-sample">
        <div class="code-header">
            <h3>Liquid 代码实现</h3>
            <button class="copy-btn" onclick="copyCode()">复制代码</button>
        </div>
        <pre><code>{% if section.settings.image_link != blank %}
  <span class="tag">&lt;a</span> <span class="attribute">href</span>=<span class="value">"{{ section.settings.image_link }}"</span> <span class="attribute">class</span>=<span class="value">"banner__image-link"</span><span class="tag">&gt;</span>
{% endif %}

{{ section.settings.image | image_url: width: 3840 | image_tag:
  width: section.settings.image.width,
  height: image_height,
  class: image_class,
  sizes: sizes,
  widths: widths,
  fetchpriority: fetch_priority
}}

{% if section.settings.image_link != blank %}
  <span class="tag">&lt;/a&gt;</span>
{% endif %}

<span class="comment">{# 相应的CSS样式确保外观一致 #}</span>
<span class="tag">.banner__image-link</span> {
  <span class="attribute">display</span>: <span class="value">block</span>;
  <span class="attribute">width</span>: <span class="value">100%</span>;
  <span class="attribute">height</span>: <span class="value">100%</span>;
  <span class="attribute">text-decoration</span>: <span class="value">none</span>;
  <span class="attribute">color</span>: <span class="value">inherit</span>;
}</code></pre>
    </div>

    <script>
        function copyCode() {
            const code = document.querySelector('.code-sample code').innerText;
            navigator.clipboard.writeText(code).then(() => {
                const btn = document.querySelector('.copy-btn');
                const originalText = btn.textContent;
                btn.textContent = '已复制!';
                setTimeout(() => {
                    btn.textContent = originalText;
                }, 2000);
            });
        }
        
        // 添加图片悬停效果
        document.querySelectorAll('.banner__image-link').forEach(link => {
            link.addEventListener('mouseover', function() {
                this.querySelector('.banner__image').style.transform = 'scale(1.05)';
            });
            
            link.addEventListener('mouseout', function() {
                this.querySelector('.banner__image').style.transform = 'scale(1)';
            });
        });
    </script>
</body>
</html>